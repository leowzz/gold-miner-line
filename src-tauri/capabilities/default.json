{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Local panel and overlay event synchronization",
  "windows": ["main", "overlay"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
