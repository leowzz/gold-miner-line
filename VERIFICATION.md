# 验证记录

## 校准空白区域鼠标穿透（2026-09-24）

- 校准控件按实际 DOM 边界上报鼠标区域：顶部拖动条、外框八个缩放区域、圆心或黄色识别框（含外伸的八个缩放点）。辅助线、提示文字及空白处不接收鼠标；黄色框内部仍可拖动整个识别框。
- Rust 每轮间隔 10ms 读取原生鼠标位置，只有穿透状态改变时更新原生窗口。鼠标按住期间保持原接收窗口，避免控件拖动中断或抢走游戏中开始的拖动；隐藏及锁定立即进入全窗口穿透。Mac 使用桌面逻辑坐标，Windows 使用物理坐标再按窗口 DPI 换算。
- Rust 16 项、前端 8 项测试通过，新增区域命中、负桌面坐标、100%/150%/200% 缩放及拖动/模式切换测试。TypeScript/Vite 构建、Rust Clippy、Mac 应用打包和 diff 检查通过。
- 浏览器模拟 IPC 验证：普通校准上报 10 个操作区域，开启识别后为 18 个；空白 DOM 命中根容器而非覆盖控件，圆心可拖出原位置，识别框移动及窗口缩小后上报新边界；锁定后区域清空，重新校准恢复。
- 已启动新版 Mac 应用，面板显示校准空白可直接点击游戏的说明，没有局部穿透初始化错误。原生跨应用点击、滚动与连续拖动未完成自动化验收，仍需手工确认；Windows 及混合 DPI 多显示器未实测。浏览器模拟不能替代原生鼠标穿透验收。

## 识别画面与轮廓预览（2026-09-24）

- 主面板显示识别框截图、真实灰色分割边界、匹配区域、夹口端点/连线和中垂线，可切换原图。校准模式也采集预览，覆盖层的识别框内保持透明，游戏方向线仍只在锁定后显示。
- 诊断图仅发送到主面板，最多 8 帧/秒，PNG 最长边限制为 384px；每帧图像、轮廓和关键点一起传输。解码完成后整帧绘制，过期 1.5 秒标记并隐藏旧标注；参数改变、隐藏或关闭识别时立即失效。
- Rust 14 项、前端 8 项测试通过；新增同帧 PNG/轮廓/关键点坐标、图像尺寸上限、歧义候选不染成已识别、校准预览及版本/过期规则测试。原有几何与配置测试保持通过。
- 使用 Rust 实际分析参考图生成诊断帧，在浏览器模拟 IPC：识别角度约 −42.9°，匹配分数 0.92；标注画面存在绿色轮廓，切换原图后对应绿色像素清除。双夹子显示「多个候选」并清空角度与分数；断流后标记过期并隐藏指标，采集失败清除旧图且显示具体原因。
- 440px、380px 宽度下无横向溢出，预览指标两列对齐，已查看窄窗口实际截图。前端构建和 Rust Clippy 检查通过。
- Mac 应用打包通过，已启动新版并开启识别；校准模式下原生预览正确显示「采集失败」「未能取得画面」及屏幕录制授权路径，保留现有自定义识别框参数。应用保持开启，便于继续校准。
- 当前 Goldline 缺少屏幕录制权限；未授予系统权限，因此真实游戏截图与 Windows 10 端到端效果仍未验收。

## 夹子识别与夹口中垂线（2026-09-24）

- 识别目标改为张开的灰色双爪夹子。检测夹口两端，取其中点与垂直方向，向矿区延长；不再检测小绳段。
- 参考图 ROI 为 (40, 95, 80, 80) 像素，检测夹口中点约 (72, 142.5)，方向约 −42.9°。匹配分数约 0.92，不代表识别正确率。离线叠加图用红色表示两端连线、黄色表示中垂线。
- Rust 12 项、前端 7 项测试通过。新增真实截图裁剪与镜像、去掉短绳、旋转至 −75°/−40°/0°/40°/75°、平移、100%/150%/200% 图像缩放、中点与垂直性断言；灰色直杆、单侧缺失、多个夹子和背景拒绝测试通过。
- 旧版小识别框扩至至少 18% × 18%，边界限制有效；验证迁移只执行一次，保留扇形和窗口配置，损坏的旧框不会触发迁移崩溃。
- 浏览器模拟 IPC：灰色容差可修改，夹口中点与方向正确传至 SVG，过期结果清除；440px/380px 面板无横向溢出，识别框参数成对对齐，查看了窄面板截图。
- TypeScript / Vite 构建、Rust Clippy、Mac 应用打包通过。已启动新版 Mac 应用并打开识别，原生面板显示夹口中垂线说明、扩大后的 18% × 18% 识别框和默认灰色容差 24，保留在校准模式。实际游戏摆动、闭合/抓取动画、多显示器及 Windows 10 尚未验证。

实机校准时需完整包含两侧夹爪和尖端，尽量避开其他灰色物体。闭合、抓住物体或更换外观可能不匹配；此时方向线应隐藏。

## 历史：矩形短线识别（2026-09-24）

- 校准模式支持识别框整体拖动、四边/四角缩放，以及面板输入左侧/顶部位置与宽高。按画面比例保存，限制在覆盖窗口内；检测不再依赖扇形轴心。
- Rust 11 项测试、前端 7 项测试通过。覆盖参考截图与镜像、平移后的短线、多角度、100%/150%/200% 图像缩放、空白/全暗/竞争线段拒绝、丢失重获、矩形边界与坐标缩放、旧径向参数迁移和矩形配置读写。
- 参考图识别框为 (85, 105, 30, 19) 像素，框内自动检测约 −43.4°。方向从检测线段上端出发，无需传入轴心。评分约 0.76，是匹配分数，不是识别正确率。
- 浏览器模拟 IPC：拖动识别框从 (40%, 20%) 到 (45%, 25%)；拖动右下角把宽高从 (10%, 8%) 改为 (15%, 13%)；拖至画面边缘不会越界，输入 95% 左侧位置自动限制为 90%（框宽 10%）。
- 440px 和 380px 面板无横向溢出，四项识别框控件成对顶沿、宽高一致，已查看面板及覆盖层截图。锁定时识别框和拖动标记消失，透明留空区域跟随矩形。
- 模拟检测结果的起点为 (46%, 27%)，实际 SVG 从该处绘制，没有使用原扇形轴心 (50%, 14%)；停止结果流后旧线清除。
- TypeScript / Vite 构建、Rust Clippy、Mac 原生应用构建通过。已启动最新 Mac 应用并开启识别，原生面板正确显示识别框四项参数及校准说明；当前保留在校准模式。浏览器模拟 IPC 和离线图片不能代替录屏权限授权后的真实游戏验证。
- Mac 实际区域截图、游戏摆动与 Windows 10 实时覆盖尚未验收。没有实现目标选点、键盘注入或自动出钩；参考 PNG 只用于测试，未加入运行时资源。

实机验收：校准黄色识别框覆盖短线摆动范围并尽量排除支架、轮子与钩爪，再锁定观察左右摆动及竖直位置。检查面板遮挡有提示、隐藏/校准时清线、关闭/重开识别不保留旧方向，并在 100%/150% 缩放下重复；确认识别框内的透明留空区阻止辅助线干扰。

## 自动检查（2026-09-23，Mac）

- 射线几何：左右对称、竖直中央线、单线偏转、归一化坐标与缩放、拖动边界。
- Rust：配置读写与原子替换、损坏配置、非法参数、部分更新、负坐标显示器及 150% 缩放下的位置恢复。
- `pnpm test`：4 项通过；`cargo test`：4 项通过。
- 前端 TypeScript / Vite 构建、`cargo check`、`cargo clippy -- -D warnings` 通过。
- `make dev` 完成编译并启动 `target/debug/goldline`；正式 Mac `.app` 构建通过。

## 平台验收

Mac 已验证：

- 成功构建并启动原生双窗口应用，查看了控制面板及射线的实际渲染。
- 面板修改数量 9 → 11，覆盖层同步变为 11 条；锁定、重新校准、隐藏和显示按钮的状态切换正常。
- 锁定后点击显示按钮，控制面板保持当前窗口，未切到覆盖层。
- 退出再启动后恢复 11 条及保存的窗口位置，并进入校准模式；恢复默认后回到 9 条。
- 控制面板设为置顶，启动时前置，避免被覆盖层挡住；点击关闭按钮后检查进程已退出。

浏览器交互检查（模拟 Tauri IPC，不能代替原生窗口验收）：

- 440px、380px 宽度下页面宽度等于视口宽度，无横向溢出；所有成对参数控件的顶沿、宽高一致。
- 单线切换与恢复默认通过；实际拖动圆心后 SVG 起点跟随更新。
- 覆盖层背景计算值为完全透明。850×563 的覆盖框默认轴心为 (425, 78.82)，对应截图去除约 57px 标题栏后、半尺寸游戏区域的预期比例。
- 原始游戏截图未加入应用资源。

仍需人工确认的 Mac 原生行为：透明窗口与下方游戏的合成效果、实际鼠标穿透、原生拖动/缩放、物理键盘全局快捷键。桌面自动化工具在覆盖窗口坐标拖动时返回 `noWindowsAvailable`；发送模拟快捷键未触发全局热键，不能据此声称这些项目已通过。浏览器截图工具也出现过超时，布局检查采用 DOM 实际尺寸和原生面板截图完成。

Windows 尚未实测。验收步骤：

1. 在 Windows 10 x64 安装 NSIS 包；缺少 WebView2 时确认安装器可引导安装。
2. 游戏窗口上确认覆盖层透明、置顶；校准拖动、四边/四角缩放及圆心拖动。
3. 锁定后点击辅助线和空白处均应操作下方游戏；显示覆盖窗口不夺取游戏键盘焦点。
4. 验证两组快捷键，以及快捷键已被其他应用占用时的面板提示。
5. 分别测试 100%、150% 和混合 DPI 双屏，含主屏左侧的负坐标显示器；移动、缩放和重启后圆心比例不漂移。
6. 断开原显示器后重启，确认覆盖窗口可见；「找回覆盖窗口」将其放回控制面板所在屏幕。
7. 修改参数后立即退出，再次启动应恢复参数和窗口位置并进入校准模式。
8. 关闭控制面板，确认覆盖窗口和进程全部退出。
